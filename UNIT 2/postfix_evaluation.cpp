//If input is operand, put it in the stack
//If input is operator, then x1=pop(), x2=pop(), result=x2 operator x1, push(result)
//By the end, the stack should only contain one value that being the result.    
//then result=pop() and print the result